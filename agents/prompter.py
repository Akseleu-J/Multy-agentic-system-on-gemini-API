from agents.base import GeminiAgent

class PromterAgent(GeminiAgent):
    def __init__(self, name: str):
        role = (
            "Ты эксперт по промт-инжинирингу. Твоя задача — взять сырое описание задачи от пользователя "
            "и превратить его в идеальный, структурированный, детальный промт для ИИ-разработчика. "
            "Выводи ТОЛЬКО получившийся текст промта, без лишних приветствий, вступлений и объяснений."
        )
        super().__init__(name, role=role, model="gemini-2.0-flash")